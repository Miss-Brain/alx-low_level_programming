#include <stdio.h>
/**
 * main - Entry point
 * code description
 * Return: Always 0 (Success)
 */

int main(void)
{
	int f = 48;

	while (f < 58)
{
	putchar(f);
	f++;
}
	putchar('\n');
	return (0);
}
